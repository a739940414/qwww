<UserControl x:Class="AlatabeSoft.Presentation.Wpf.Views.CustomersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="900"
             d:DesignHeight="600"
             FlowDirection="RightToLeft">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="العملاء والموردون"
                       FontSize="20"
                       FontWeight="Bold"
                       Margin="0,0,0,16" />

            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="النوع" Margin="0,0,0,6" />
                <ComboBox Grid.Row="1" Grid.Column="0" ItemsSource="{Binding EntityTypes}" SelectedItem="{Binding SelectedEntityType}" />

                <TextBlock Grid.Row="0" Grid.Column="1" Text="العملة" Margin="0,0,0,6" />
                <ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding Currencies}" SelectedItem="{Binding SelectedCurrency}" />

                <TextBlock Grid.Row="2" Grid.Column="0" Text="الاسم" Margin="0,16,0,6" />
                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Row="3" Grid.Column="0" Text="الهاتف" Margin="0,16,0,6" />
                <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Row="4" Grid.Column="0" Text="البريد الإلكتروني" Margin="0,16,0,6" />
                <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Row="5" Grid.Column="0" Text="العنوان" Margin="0,16,0,6" />
                <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding Address}" AcceptsReturn="True" Height="60" />

                <TextBlock Grid.Row="6" Grid.Column="0" Text="حد الائتمان" Margin="0,16,0,6" />
                <TextBox Grid.Row="6" Grid.Column="1" Text="{Binding CreditLimit, UpdateSourceTrigger=PropertyChanged}" />
            </Grid>

            <Button Content="حفظ"
                    Width="140"
                    HorizontalAlignment="Right"
                    Margin="0,12,0,0"
                    Command="{Binding SaveEntityCommand}" />
        </StackPanel>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Entities}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  Margin="0,20,0,0"
                  FlowDirection="RightToLeft">
            <DataGrid.Columns>
                <DataGridTextColumn Header="النوع" Binding="{Binding TypeDisplay}" Width="*" />
                <DataGridTextColumn Header="الاسم" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="الهاتف" Binding="{Binding Phone}" Width="*" />
                <DataGridTextColumn Header="البريد" Binding="{Binding Email}" Width="*" />
                <DataGridTextColumn Header="العملة" Binding="{Binding CurrencyCode}" Width="*" />
                <DataGridTextColumn Header="حد الائتمان" Binding="{Binding CreditLimit, StringFormat={}{0:N2}}" Width="*" />
                <DataGridTextColumn Header="الرصيد" Binding="{Binding Balance, StringFormat={}{0:N2}}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
